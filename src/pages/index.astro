---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout title="Home - Bharat Sharma">
  <!-- Hero Section - Clean & Glassy -->
  <section class="min-h-[85vh] flex items-center justify-center relative py-12 homepage-alive">
    <div class="w-full max-w-5xl mx-auto z-10 px-4">
      
      <!-- Hero Content -->
      <div class="text-center mb-16 space-y-6">
        <!-- Name with subtle acrylic effect -->
        <div class="glass-panel rounded-3xl px-16 py-12 shadow-glass inline-block mx-auto">
          <h1 class="text-6xl md:text-7xl font-bold mb-3" style="color: #1a1a1a; letter-spacing: -0.03em;">
          Bharat Sharma
        </h1>
          <p class="text-xl md:text-2xl font-medium" style="color: #0078d4;">
          Programmer & Tech Writer
        </p>
      </div>

        <!-- Subtitle -->
        <p class="text-lg max-w-2xl mx-auto leading-relaxed" style="color: #505050;">
          Building innovative software solutions across a wide range of technologies
        </p>

      <!-- CTA Buttons -->
        <div class="flex gap-4 justify-center mt-10 flex-wrap">
          <a href="/blog" class="glass-button">
          Read Blog
        </a>
          <a href="/about" class="glass-button">
          About Me
        </a>
      </div>
      </div>

      <!-- Hero Page Decorative Bubbles - Extra glossy -->
      <div class="absolute top-20 left-10 w-40 h-40 rounded-full animate-float pointer-events-none" style="background: radial-gradient(circle at 30% 30%, rgba(150, 240, 255, 0.6), rgba(100, 200, 255, 0.35) 50%, transparent 80%); box-shadow: 0 12px 48px rgba(100, 220, 255, 0.4), inset -12px -12px 24px rgba(255, 255, 255, 0.5), inset 12px 12px 24px rgba(0, 150, 255, 0.2); filter: blur(0.5px);"></div>
      <div class="absolute bottom-32 right-16 w-56 h-56 rounded-full animate-float pointer-events-none" style="animation-delay: 2s; background: radial-gradient(circle at 30% 30%, rgba(180, 255, 255, 0.55), rgba(120, 220, 255, 0.3) 50%, transparent 80%); box-shadow: 0 16px 64px rgba(120, 240, 255, 0.35), inset -16px -16px 32px rgba(255, 255, 255, 0.45), inset 16px 16px 32px rgba(0, 180, 255, 0.18); filter: blur(0.5px);"></div>
      <div class="absolute top-1/3 right-1/4 w-32 h-32 rounded-full animate-float pointer-events-none" style="animation-delay: 4s; background: radial-gradient(circle at 30% 30%, rgba(160, 235, 255, 0.5), rgba(110, 210, 255, 0.28) 50%, transparent 80%); box-shadow: 0 10px 40px rgba(110, 225, 255, 0.3), inset -10px -10px 20px rgba(255, 255, 255, 0.45), inset 10px 10px 20px rgba(0, 170, 255, 0.15); filter: blur(0.5px);"></div>
      <div class="absolute top-1/2 left-1/3 w-28 h-28 rounded-full animate-float pointer-events-none" style="animation-delay: 6s; background: radial-gradient(circle at 30% 30%, rgba(140, 225, 255, 0.48), rgba(90, 190, 255, 0.25) 50%, transparent 80%); box-shadow: 0 8px 36px rgba(100, 215, 255, 0.28), inset -8px -8px 18px rgba(255, 255, 255, 0.4), inset 8px 8px 18px rgba(0, 160, 255, 0.12); filter: blur(0.5px);"></div>
    </div>
  </section>

  <!-- Featured Section with Enhanced Glass Cards -->
  <section class="mt-20 mb-20">
    <div class="grid md:grid-cols-3 gap-8">
    <!-- Card 1 - Development -->
      <div class="glass-panel rounded-3xl p-10 group hover-lift cursor-pointer relative">
        <div class="relative z-10">
          <div class="w-20 h-20 mb-6 rounded-2xl bg-gradient-to-br from-blue-400 via-blue-500 to-cyan-500 flex items-center justify-center group-hover:scale-115 group-hover:rotate-8 transition-all duration-500 shadow-xl border-3 border-white/50" style="box-shadow: 0 8px 32px rgba(59, 130, 246, 0.45), inset 0 2px 8px rgba(255, 255, 255, 0.6), inset 0 -2px 8px rgba(59, 130, 246, 0.3);">
            <div class="absolute inset-0 rounded-2xl bg-gradient-to-br from-white/60 via-white/30 to-transparent"></div>
            <div class="absolute inset-0 rounded-2xl shadow-inner"></div>
            <svg class="w-10 h-10 text-white relative z-10 drop-shadow-lg" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M10 20l4-16m4 4l4 4-4 4M6 16l-4-4 4-4"/>
            </svg>
          </div>
          <h3 class="text-2xl font-bold mb-3 group-hover:text-blue-600 transition-colors duration-300" style="color: #1a1a1a;">
        Development
      </h3>
          <p class="leading-relaxed text-base" style="color: #505050;">
        Building software across diverse tech stacks and programming languages.
      </p>
        </div>
    </div>

    <!-- Card 2 - Tech Writing -->
      <div class="glass-panel rounded-3xl p-10 group hover-lift cursor-pointer relative">
        <div class="relative z-10">
          <div class="w-20 h-20 mb-6 rounded-2xl bg-gradient-to-br from-blue-400 via-blue-500 to-cyan-500 flex items-center justify-center group-hover:scale-115 group-hover:rotate-8 transition-all duration-500 shadow-xl border-3 border-white/50" style="box-shadow: 0 8px 32px rgba(59, 130, 246, 0.45), inset 0 2px 8px rgba(255, 255, 255, 0.6), inset 0 -2px 8px rgba(59, 130, 246, 0.3);">
            <div class="absolute inset-0 rounded-2xl bg-gradient-to-br from-white/60 via-white/30 to-transparent"></div>
            <div class="absolute inset-0 rounded-2xl shadow-inner"></div>
            <svg class="w-10 h-10 text-white relative z-10 drop-shadow-lg" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M11 5H6a2 2 0 00-2 2v11a2 2 0 002 2h11a2 2 0 002-2v-5m-1.414-9.414a2 2 0 112.828 2.828L11.828 15H9v-2.828l8.586-8.586z"/>
            </svg>
          </div>
          <h3 class="text-2xl font-bold mb-3 group-hover:text-blue-600 transition-colors duration-300" style="color: #1a1a1a;">
        Tech Writing
      </h3>
          <p class="leading-relaxed text-base" style="color: #505050;">
        Sharing insights on programming, software engineering, and emerging technologies.
      </p>
        </div>
    </div>

    <!-- Card 3 - Open Source -->
      <div class="glass-panel rounded-3xl p-10 group hover-lift cursor-pointer relative">
        <div class="relative z-10">
          <div class="w-20 h-20 mb-6 rounded-2xl bg-gradient-to-br from-blue-400 via-blue-500 to-cyan-500 flex items-center justify-center group-hover:scale-115 group-hover:rotate-8 transition-all duration-500 shadow-xl border-3 border-white/50" style="box-shadow: 0 8px 32px rgba(59, 130, 246, 0.45), inset 0 2px 8px rgba(255, 255, 255, 0.6), inset 0 -2px 8px rgba(59, 130, 246, 0.3);">
            <div class="absolute inset-0 rounded-2xl bg-gradient-to-br from-white/60 via-white/30 to-transparent"></div>
            <div class="absolute inset-0 rounded-2xl shadow-inner"></div>
            <svg class="w-10 h-10 text-white relative z-10 drop-shadow-lg" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M21 12a9 9 0 01-9 9m9-9a9 9 0 00-9-9m9 9H3m9 9a9 9 0 01-9-9m9 9c1.657 0 3-4.03 3-9s-1.343-9-3-9m0 18c-1.657 0-3-4.03-3-9s1.343-9 3-9m-9 9a9 9 0 019-9"/>
            </svg>
          </div>
          <h3 class="text-2xl font-bold mb-3 group-hover:text-blue-600 transition-colors duration-300" style="color: #1a1a1a;">
        Open Source
      </h3>
          <p class="leading-relaxed text-base" style="color: #505050;">
        Contributing to open-source projects across various programming ecosystems.
      </p>
        </div>
      </div>
    </div>
  </section>
</BaseLayout>

<style>
  /* Floating animation for decorative elements */
  @keyframes float {
    0%, 100% {
      transform: translateY(0) scale(1) rotate(0deg);
    }
    33% {
      transform: translateY(-25px) scale(1.12) rotate(2deg);
    }
    66% {
      transform: translateY(-15px) scale(1.08) rotate(-2deg);
    }
  }

  .animate-float {
    animation: float 8s ease-in-out infinite;
  }

  /* Enhanced hover lift effect */
  .hover-lift {
    transition: all 0.5s cubic-bezier(0.4, 0, 0.2, 1);
  }

  .hover-lift:hover {
    transform: translateY(-12px) scale(1.02);
    box-shadow: 
      0 25px 50px rgba(0, 0, 0, 0.1),
      0 0 0 1px rgba(255, 255, 255, 0.6) inset,
      0 0 30px rgba(59, 130, 246, 0.15);
  }

  /* Homepage Alive Effect - Shifting elements */
  .homepage-alive {
    position: relative;
  }

  .homepage-alive::before {
    content: '';
    position: absolute;
    inset: 0;
    background: radial-gradient(circle at 50% 50%, 
      rgba(180, 240, 255, 0.15) 0%, 
      transparent 70%);
    animation: pulse-glow 6s ease-in-out infinite;
    pointer-events: none;
    z-index: 0;
  }

  @keyframes pulse-glow {
    0%, 100% {
      opacity: 0.3;
      transform: scale(1);
    }
    50% {
      opacity: 0.6;
      transform: scale(1.2);
    }
  }
</style>
